let numeros: number[] = [2.5, 2, 0.1]
let nomes: Array<string> = ['José', '2', 'Maria', 'Francis']

let nome: string
nome = 'Jean'

nomes.push(nome) // add elemento na lista

nomes.splice(1, 1)
numeros.pop()
console.log(nomes[0])
console.log(numeros)
console.log(nomes)